server:
  port: ":8080"

rate_limiter:
  max_requests_per_second: 5
  burst_size: 10

cache:
  max_size: 100
  default_ttl: "1m"

  mongo:
    uri: "mongodb://root:example@localhost:27017/"
    database: "usersdb"
    collection: "users"